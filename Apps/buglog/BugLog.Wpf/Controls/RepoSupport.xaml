<UserControl x:Class="BugLog.Wpf.Controls.RepoSupport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             xmlns:local="clr-namespace:BugLog.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.RowSpan="2" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Width="32"  Height="32" BorderBrush="Transparent" Background="Transparent" Command="{Binding NewRepoCommand}">
                <fa5:SvgAwesome Icon="Solid_Plus" Width="24" Height="24" />
            </Button>

            <ListView Grid.Row="1" ItemsSource="{Binding Repos}" SelectedItem="{Binding Repo, Mode=TwoWay}"  >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding Name}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Column="1" >
            <Button Command="{Binding GitCommand}" CommandParameter="status"  >GIT Status</Button>
        </StackPanel>
        <ListView ItemsSource="{Binding ConsoleLogOutput}" Background="Blue" Grid.Row="1" Grid.Column="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Output}" Foreground="Yellow" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <Grid Width="320" Height="200" Grid.ColumnSpan="2" Grid.RowSpan="2" Visibility="{Binding Repo, Converter={StaticResource VisibilityConverter}}" >
            <Rectangle Style="{StaticResource Popup}" />
            
            <Grid Style="{StaticResource PopupContent}">
                <StackPanel >
                    <TextBlock Text="Repo Name" />
                    <TextBox Text="{Binding Repo.Name}" />

                    <TextBlock Text="Path" />
                    <TextBox Text="{Binding Repo.Folder}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button   Command="{Binding SaveRepoCommand}" >Save</Button>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Border Style="{StaticResource PopupTitleBorder}" VerticalAlignment="Top"/>
            <TextBlock Text="Repository" Foreground="White" Margin="5,5,0,0" />
            <Button Command="{Binding CancelRepoCommand}" Style="{StaticResource PopupCloseButton}" />

        </Grid>
    </Grid>
</UserControl>
